<UserControl
    x:Class="HostComputer.Views.Overview.Overview_SoftwareView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:HostComputer.Views.Overview"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Border Padding="20"
            Background="#F7F9FC"
            CornerRadius="8">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock
                Text="Software Version"
                FontSize="22"
                FontWeight="Bold"
                Foreground="#333"/>

            <!-- Content -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Software Name -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Software Name :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SoftwareName}"/>

                <!-- Software Version -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Software Version :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SoftwareVersion}"/>

                <!-- PLC Firmware -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="PLC Firmware Version :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PlcFirmwareVersion}"/>

                <!-- Git Revision -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Git Revision :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding GitRevision}"/>

                <!-- Build Time -->
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Build Time :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding BuildTime}"/>

                <!-- Last Modified -->
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Commit Message :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding CommitMessage}"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
